# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['quantum_gui.py'],
    pathex=[],
    binaries=[],
    datas=[('main_menu.ui', '.'), ('imf_window.ui', '.'), ('mimaf_in_window.ui', '.'), ('mimaf_view.ui', '.'), ('utilities.ui', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='quantum_gui',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='quantum_gui',
)
